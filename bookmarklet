javascript:(function () {
    data = {
	title: document.title,
	url: window.location.href,
	auto_close: true
    };

    urlParams = new URLSearchParams(data);
    newUrl = new URL("http://dpins.link/add");
    newUrl.search = "?" + urlParams.toString();
    window.open(newUrl.toString());
})();
